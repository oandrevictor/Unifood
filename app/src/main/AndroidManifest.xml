<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.unifood">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activities.LoginActivity"
            android:label="Logar-se">
        </activity>
        <!--
 ATTENTION: This was auto-generated to add Google Play services to your project for
     App Indexing.  See https://g.co/AppIndexing/AndroidStudio for more information.
        -->
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <activity android:name=".activities.SignUpActivity" />
        <activity android:name=".activities.AdminUniversityActivity" />
        <activity android:name=".activities.CampusActivity" />
        <activity android:name=".activities.UniversityActivity" />
        <activity android:name=".activities.RestaurantActivity" />
        <activity android:name=".activities.RestaurantRegisterActivity" />
        <activity android:name=".activities.StudentHomeActivity" />
        <activity android:name=".activities.RestaurantHomeActivity" />
        <activity android:name=".activities.StudentEditActivity" />
        <activity android:name=".activities.RestaurantEditActivity" />
        <activity android:name=".activities.ProductActivity"></activity>
    </application>

</manifest>